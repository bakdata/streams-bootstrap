{{ if .Values.files }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "consumerproducer-app.fullname" . }}
  {{- include "consumerproducer-app.annotations" . }}
  labels:
    {{- include "consumerproducer-app.labels" . | nindent 4 }}
    {{- range $key, $value := .Values.labels }}
    {{ $key | quote }}: {{ $value | quote }}
    {{- end }}
data:
  {{- range $key, $value := .Values.files }}
  {{ $key }}: {{ $value.content | quote }}
  {{- end }}
{{ end }}
